Practical 2A
AIM : Study of list,tuple and Dictionary
Functions used: print(), list_name = [list], .append(), .insert(), print(list_name[position]), list_name[position]="term_to_be_added",
                len(list_name), list1.extend(list2), .remove(), .sort(), min(), max(), sum().
Theory : 1.List creation:
A list is created using square brackets [] to store multiple ordered items.

2.Printing a list:
print() displays all elements of the list in sequence.

3.Duplicate elements in list:
Lists allow duplicate values.

4.List iteration (for loop):
A for loop is used to access each element of a list one by one.

5.len() function:
len() returns the total number of elements in a list.

6.Heterogeneous list:
A list can store different data types like string, integer, float, and boolean.

7.append() method:
append() adds a new element at the end of the list.

8.Positive indexing:
List elements can be accessed using index starting from 0.

9.Negative indexing:
Negative indexes access elements from the end of the list.

10.index() method:
index() finds the position of a specified element in the list.

11.Element replacement:
A list element can be replaced by assigning a new value to its index.

12.List concatenation:
Two lists can be combined using the + operator.

13.List slicing:
Slicing extracts a portion of a list using start and end indexes.

14.Accessing first element:
Index 0 gives the first element of the list.

15.Accessing last element:
Index -1 gives the last element of the list.

16.remove() method:
remove() deletes a specific element from the list.

17.sorted() function:
sorted() arranges list elements in ascending (alphabetical) order.

18.max() function:
max() returns the highest value from the list.

19.min() function:
min() returns the lowest value from the list.

20.sum() function:
sum() calculates the total of numeric list elements.

21.Average calculation:
Average is calculated by dividing total marks by number of students.

22.Conditional statement:
if–else prevents errors like division by zero.

23.Sorting numeric list:
sorted() arranges numeric values in increasing order.

Algorithm : 
1.Start
2.Create a list of elements
3.Display the list
4.Add duplicate elements and display the list
5.Find the length of the list using len()
6.Add new elements using append()
7.Access elements using positive and negative indexing
8.Replace an element using its index
9.Combine two lists using + operator
10.Extract elements using slicing
11.Add and remove elements from the list
12.Sort the list alphabetically
13.Create a list of student marks
14.Find maximum and minimum marks
15.Calculate average marks using sum and length
16.Sort the marks list
17.Display all results
18.Stop

Conclusion : Successfully coded and executed list using different type of functions, with vast idea of executing the code.
